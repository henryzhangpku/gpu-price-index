__pycache__/
*.pyc
.venv/
data/*.db
data/*.db-journal
.pytest_cache/
*.egg-info/

# Generated by `gpuidx export-web` at deploy time; never committed, so the
# site cannot carry a stale second copy of the tape.
web/data/

# Google Fonts TTFs cached by scripts/make_og.py; refetched on demand.
scripts/.fonts/
